[project]
name = "yandex-weather-mcp"
version = "0.1.0"
description = "MCP server with date and time tools using FastMCP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=0.1.0",
]
